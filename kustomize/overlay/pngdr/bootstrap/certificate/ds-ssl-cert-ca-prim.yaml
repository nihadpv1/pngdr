apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ds-ssl-cert
  namespace: pngdr
spec:
  commonName: ds

  dnsNames:
    - "*.ds"
    - "*.ds-idrepo"
    - "*.ds-cts"
    - "ds-idrepo-0.ds-idrepo.pngdr.svc.cluster.local"
    - "ds-idrepo-1.ds-idrepo.pngdr.svc.cluster.local"
    - "drlonvspngdrw1.arabbanking.local"
    - "drlonvspngdrw2.arabbanking.local"
    

  duration: 175200h

  isCA: false

  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: pngprddr-ca-issuer

  privateKey:
    algorithm: ECDSA
    rotationPolicy: Never

  secretName: ds-ssl-keypair

  secretTemplate:
    annotations:
      my-secret-annotation: cert-manager-generated
    labels:
      app: ds

  subject:
    organizations:
      - forgerock.org

  usages:
    - server auth
    - client auth
